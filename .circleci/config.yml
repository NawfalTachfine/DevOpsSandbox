version: 2.1
# ---
jobs:
  fail:
    docker: 
      - image: circleci/node:13.8.0
    steps:
      - run: return 1 # echo "Not failing!"
      - when: 
          condition: on_fail
          steps:
            - run: echo "Nevermind I'm fine!"
# ---
workflows:
  handle_failure:
    jobs:
      - fail
